# 🌙 QuranFlow Mobile Integration

## Integration with ADAN-ID OpenCloud

This directory contains mobile application components for QuranFlow that integrate with:

- **Biometric Authentication Service** (`/auth/biometric/login`)
- **Storage Service** (`/storage/upload`, `/storage/download/:id`)
- **Security Vault** (`/vault/abjad/calculate`)

## Features
- Voice-to-Quran dictation
- Tajweed correction with Jannah Points
- Biometric identity verification
- Encrypted storage via Firebase + IPFS
